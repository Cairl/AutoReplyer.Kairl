# 2026-06-19

## AutoReplyer.Kairl — 3个Bug修复

### 修复内容
1. **全局监控开关 (tui.py)**：在群列表上方渲染全局监控开关（●/○ + Enter 切换），删除旧的 per-group toggle，导航从 %4 修正为 %3
2. **多显示器DPI框选错位 (region.py)**：移植 overlay.py 的 `_set_dpi_aware()` + `_virtual_screen_rect()`，用 geometry() 替代 -fullscreen，坐标统一为物理像素
3. **红框框选气泡 (monitor.py)**：overlay.show() 从框选整个 message_region 改为框选检测到的聊天气泡（绝对坐标转换）

### 额外改进
4. **回复用时 (monitor.py + tui.py)**：stats 新增 `reply_elapsed` 字段，tui 显示 "回复用时" + 秒数差值

### 修改文件
- `ui/tui.py`
- `core/region.py`
- `core/monitor.py`

### 测试
- QA 35/35 测试通过，Route: NoOne
- 团队: software-autoreplyer-fix (工程师 + QA)

---

## AutoReplyer.Kairl — region.py 截取位置报错修复（第二次修复）

### 问题
用户使用"截取位置"功能时报错，错误一闪而过无法捕获。

### 修复（5个Bug + 3处错误日志）
1. `QApplication([])` → `QApplication(sys.argv)`（L78）
2. `np.array()` → `np.ascontiguousarray()` 强制 C-contiguous（L94-96）
3. `w * 3` → `int(rgb.strides[0])` 用真实 stride（L167）
4. `_Overlay()` 构造包 try/except 防僵尸窗口（L287-291）
5. `app.exec()` 包 try/except（L301-304）
6. 新增 `_log_error()` 写 stderr+flush（L51-53）
7. `pyautogui.screenshot()` 包 try/except（L92-98）

### 修改文件
- `core/region.py`

### 测试
- QA 静态审查全部通过（5个修复+3处错误处理），Route: NoOne
- 团队: software-bugfix-region-crash (工程师 + QA)
