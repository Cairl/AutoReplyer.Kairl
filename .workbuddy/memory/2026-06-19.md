# 2026-06-19

## TUI Refactoring — Merge Monitor into Main + Unify Settings
- Refactored `main.py` from 5 screens (MAIN, REPLY, GROUPS, GROUP_ACTIONS, MONITOR) to 3 screens (MAIN, SETTINGS, GROUP_ACTIONS).
- MAIN panel now shows monitor stats inline + direct toggle button + unified menu.
- SETTINGS combines reply settings and group management visually with section dividers.
- Engineer (software-engineer) completed code; QA (software-qa-engineer) verified all 8 tests passed.
- All screen transitions, reply editing, group management, and monitor integration preserved.

## UI Fixes Round — Remove Toggle, Decrowd Stats, Fix Borders
- Removed "停止监控"/"启动监控" toggle button from MAIN (exit already stops monitor).
- Stats panel expanded from 2 cramped lines to 4 lines (status, counters, last_trigger, last_reply).
- Added ML/MR = "├"/"┤" box-drawing chars; replaced internal separators from `────` to `├──┤`.
- Fixed `box_line()` right border color: re-applies `{C.GRAY}` before closing `│` to prevent ANSI reset interference.
- Engineer fixed all 3 issues; QA verified 6/6 tests passed.

## UI Polish — Remove Hint Lines + Backspace Navigation
- Removed `[Up/Down] 导航 [Enter] 确认 [Esc] 退出` hint lines from all 3 screens.
- Added Backspace (`b"\x08"`) as back/exit shortcut: MAIN→exit, SETTINGS→MAIN, GROUP_ACTIONS→SETTINGS.
- In SETTINGS, backspace navigation short-circuits before `reply_editing` check so text-deletion backspace still works.
- QA verified 4/4 tests passed.

## MAIN Panel Visual Polish — Title in Border + Separator Spacing
- Removed subtitle "微信群 @所有人 自动回复工具".
- Title "AutoReplyer.Kairl" embedded into top border: `╭─── AutoReplyer.Kairl ────╮`.
- Internal separators changed from full-width `├────────┤` to `├  ────────  ┤` with 2-space internal padding.
- Other panels (SETTINGS, GROUP_ACTIONS) separators unchanged.
- QA verified 5/5 tests passed.

## Menu Text Readability — C.WHITE on unselected items
- Wrapped all unselected menu/button text in `{C.WHITE}` across MAIN, SETTINGS, GROUP_ACTIONS.
- Selected items (`›{C.BOLD}`) untouched — clean separation: bold=selected, WHITE=unselected.
- 4 locations: MAIN items, SETTINGS group names + add/back buttons, GROUP_ACTIONS action labels.

## Visual Design Alignment — Dashed Separators + Sub-menu Layout + Polish
- Internal separators changed to dashed `╌` (HD constant) across all 3 panels. Top/bottom borders stay solid.
- SETTINGS and GROUP_ACTIONS titles embedded in top borders (matching MAIN style).
- SETTINGS section header "── 群管理 ──" embedded in internal dashed separator.
- Removed scan count from MAIN stats counters (only trigger/reply/error shown).
- C.GRAY brightened from #6C7086 → #9498B2 for border readability.
- MR (┤) removed from all internal separator lines — only ├ remains on left side.
- QA verified 6/6 tests passed.

## Single-Panel Refactor — All Content Merged into MAIN
- Removed all sub-screens (SETTINGS, GROUP_ACTIONS). Entire app is one scrollable MAIN panel.
- Removed screen-switching architecture: no `self.screen`, no `_get_render_lines()` dispatch, no `_handle_key()` branching.
- Reply settings, group management, add group, and exit all reside in `_render_main()` / `_handle_main()`.
- Group actions converted to inline overlay modal (4 actions appear below selected group).
- All separators use plain `box_line()` with dashed content — NO ├ or ┤ characters in render output.
- Section headers "── 回复设置 ──" and "── 群管理 ──" rendered via `box_line()`.
- Navigation: single `self.main_selected` cursor over `4 + len(groups) + 2` items.
- QA verified 8/8 tests passed. Config/Monitor/RegionSelector classes untouched.
